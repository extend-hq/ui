---
title: E-Signature
description: Place, review, and complete signature fields on PDF documents.
component: true
---

E-Signature extends a PDF viewer with signature, initials, date, and approval fields. It should keep field placement precise while making signing status clear.

See it composed with the PDF Viewer in the [E-Signature block](/blocks#e-signature).

<ESignatureDemo />

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add @extend/e-signature
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>Install the following dependencies:</Step>

```bash
npm install pdf-lib@^1.17.1 signature_pad@^5.1.3
```

<Step>Install the icon library configured in your `components.json`:</Step>

The component source references icons through the shadcn CLI's
`IconPlaceholder`, which supports every `iconLibrary` the CLI does: `lucide`,
`tabler`, `hugeicons`, `phosphor`, and `remixicon`. Installing with the CLI
replaces each placeholder with the matching icon from your configured library,
so make sure that library's package is installed. When copying the source
manually, replace each `IconPlaceholder` element with the icon named by the
prop for your library.

<Step>Copy and paste the following code into your project.</Step>

<ESignatureSource />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Useful behaviors

- Signature, initials, date, checkbox, and text field placement.
- Recipient-specific field ownership and completion state.
- Required-field validation before submit.
- Audit-friendly signed, pending, and declined states.

## API Reference

### ESignatureBlock

The ESignatureBlock component renders a PDF signing workflow with a document preview, signature field panel, signature dialog, and signed-PDF export.

| Prop   | Type     | Default | Required |
| ------ | -------- | ------- | -------- |
| `file` | `string` | `-`     | No       |
